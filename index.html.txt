import React, { useState, useEffect, useRef } from 'react';
import { 
  ArrowRight, 
  Calculator, 
  TrendingDown, 
  Clock, 
  Lock, 
  AlertTriangle, 
  Smartphone, 
  Check, 
  ShieldCheck, 
  CheckCircle2, 
  Award, 
  Users,
  Play,
  RefreshCw,
  TrendingUp,
  Shield,
  Percent,
  Flame,
  UserCheck
} from 'lucide-react';

export default function App() {
  // --- Состояния Срочности и Дефицита (Scarcity & Urgency) ---
  const [timeLeft, setTimeLeft] = useState(900); // 15 минут обратного отсчета (evergreen)
  const [seatsLeft, setSeatsLeft] = useState(3);

  // --- Состояния интерактивных блоков ---
  const [leadsCount, setLeadsCount] = useState(400);
  const [staffCount, setStaffCount] = useState(3);
  const [staffSalary, setStaffSalary] = useState(350000); // KZT
  const [leadValue, setLeadValue] = useState(30000); // KZT
  const [activeStage, setActiveStage] = useState(1);
  const [vslPlaying, setVslPlaying] = useState(false);

  // --- Состояния Двухэтапного Оформления Заказа (2-Step Checkout with Bump Offer) ---
  const [checkoutModal, setCheckoutModal] = useState(false);
  const [checkoutStep, setCheckoutStep] = useState('intake'); // intake -> billing -> success
  const [leadName, setLeadName] = useState('');
  const [leadPhone, setLeadPhone] = useState('');
  const [companyName, setCompanyName] = useState('');
  const [consentPDPL, setConsentPDPL] = useState(false);
  const [consentAI, setConsentAI] = useState(false);
  
  // Bump Offer State (Дополнительный импульсный оффер)
  const [bumpSelected, setBumpSelected] = useState(false);
  const bumpPrice = 15000; // Цена бамп-оффера
  const basePrice = 99970;

  // --- Симуляция Консоли n8n ---
  const [logs, setLogs] = useState([
    '[SYSTEM] Инициализация протокола Sheberlik.ai... Безопасный контур PS.KZ активен.',
    '[SYS_LOG] Обнаружен целевой B2B-трафик из Алматы. Блокировка путей отхода включена.'
  ]);

  const addLog = (message) => {
    setLogs((prev) => [...prev.slice(-3), `[n8n_PIPELINE] ${message}`]);
  };

  // --- Таймер обратного отсчета (Evergreen) ---
  useEffect(() => {
    const timer = setInterval(() => {
      setTimeLeft((prev) => {
        if (prev <= 1) {
          addLog('Вечнозеленый таймер обновлен для нового сеанса удержания внимания.');
          return 900;
        }
        return prev - 1;
      });
    }, 1000);
    return () => clearInterval(timer);
  }, []);

  // --- Имитация динамического дефицита мест ---
  useEffect(() => {
    const interval = setInterval(() => {
      setSeatsLeft((prev) => {
        if (prev > 1) {
          const nextSeats = prev - 1;
          addLog(`Внимание: Изменение дефицита! Доступных слотов на Алматы осталось: ${nextSeats}`);
          return nextSeats;
        }
        return prev;
      });
    }, 45000);
    return () => clearInterval(interval);
  }, []);

  // Форматирование времени в ММ:СС
  const formatTime = (seconds) => {
    const m = Math.floor(seconds / 60);
    const s = seconds % 60;
    return `${String(m).padStart(2, '0')}:${String(s).padStart(2, '0')}`;
  };

  // --- Математика калькулятора ROI ---
  const monthlyStaffOverhead = staffCount * staffSalary;
  const leakedLeads = Math.round(leadsCount * 0.30); // 30% слив лидов человеком
  const lostRevenue = leakedLeads * leadValue;
  const totalCurrentCost = monthlyStaffOverhead + lostRevenue;
  const netSavings = totalCurrentCost - basePrice;

  // Референс для скролла
  const calculatorRef = useRef(null);
  const valueStackRef = useRef(null);

  // --- Обработка клика по триал-клоузу ---
  const handleTrialCloseYes = () => {
    addLog('Событие: Клик "ДА" в FAB-слайдере. Мгновенная активация Step 1 Checkout.');
    openCheckout();
  };

  const handleTrialCloseNo = () => {
    if (activeStage < 5) {
      const next = activeStage + 1;
      setActiveStage(next);
      addLog(`Событие: Клик "Нет" в триал-клоузе. Переход к следующей параболе убеждения: Этап ${next}.`);
    } else {
      setActiveStage(1);
      addLog('Событие: Слайдер завершен. Перезапуск цикла убеждения.');
    }
  };

  // --- Управление Двухэтапным оформлением ---
  const openCheckout = () => {
    setCheckoutStep('intake');
    setCheckoutModal(true);
    addLog('Модальное окно открыто. Шаг 1: Ожидание сбора контактных данных в amoCRM.');
  };

  // Шаг 1: Отправка контактов в CRM
  const handleIntakeSubmit = (e) => {
    e.preventDefault();
    if (!consentPDPL || !consentAI) return;
    
    addLog(`[CRM_DISPATCH] Передача лида: Имя: ${leadName}, Тел: ${leadPhone}, Компания: ${companyName}`);
    addLog('Данные успешно сохранены в amoCRM (Статус: Неразобранное / Попытка оплаты). Потеря лида исключена.');
    
    setCheckoutStep('billing');
    addLog('Шаг 2: Инициализация платежного шлюза Kaspi Pay.');
  };

  // Шаг 2: Симуляция оплаты
  const simulatePaymentSuccess = () => {
    addLog('[WEBHOOK_RECEIVE] Каспи эквайринг зафиксировал успешную оплату.');
    setCheckoutStep('success');
    addLog('[ONBOARDING] Сценарий n8n отправил соглашения в WhatsApp. Доступ предоставлен.');
  };

  return (
    <div className="min-h-screen bg-[#0D1117] text-[#E6E6E6] font-sans selection:bg-[#A51D36] selection:text-white pb-24 relative overflow-hidden">
      
      {/* Световые градиенты (Dark Liquid Tech) */}
      <div className="absolute top-[-10%] left-[-10%] w-[550px] h-[550px] bg-red-950/20 blur-[130px] rounded-full pointer-events-none mix-blend-screen"></div>
      <div className="absolute bottom-[-10%] right-[-10%] w-[550px] h-[550px] bg-blue-950/15 blur-[130px] rounded-full pointer-events-none mix-blend-screen"></div>

      {/* ШАПКА БЕЗ НАВИГАЦИОННЫХ ССЫЛОК И УТЕЧЕК (Navigation Leak Shield) */}
      <header className="w-full py-5 px-6 border-b border-white/5 bg-[#0D1117]/95 backdrop-blur-md sticky top-0 z-40 flex justify-between items-center select-none">
        <div className="flex items-center gap-2">
          <div className="w-2.5 h-2.5 rounded-full bg-[#A51D36] animate-pulse"></div>
          <span className="text-xl font-extrabold tracking-tighter text-white uppercase">
            Sheberlik.<span className="text-transparent bg-clip-text bg-gradient-to-r from-red-500 to-orange-500">ai</span>
          </span>
        </div>
        <div className="flex items-center gap-6">
          <div className="flex items-center gap-2 text-xs font-mono text-gray-500 bg-black/40 px-3 py-1 rounded-full border border-white/5">
            <Clock className="w-3.5 h-3.5 text-[#A51D36] animate-pulse" />
            <span>ДО СБРОСА ЦЕНЫ: <strong className="text-white font-mono">{formatTime(timeLeft)}</strong></span>
          </div>
          <div className="hidden md:flex items-center gap-2 text-xs font-mono text-gray-500">
            <span>СЕРВЕР: PS.KZ (АЛМАТЫ)</span>
          </div>
        </div>
      </header>

      {/* ГЛАВНЫЙ КОНТЕНТ ЛЕНДИНГА */}
      <main className="max-w-4xl mx-auto px-4 sm:px-6">

        {/* 10X HOOK & HEADER (HDR) */}
        <section className="pt-16 pb-12 text-center animate-fade-in">
          <div className="inline-flex items-center gap-2 px-3 py-1.5 rounded-full bg-[#A51D36]/10 border border-[#A51D36]/20 text-[#A51D36] text-xs font-black mb-8 uppercase tracking-widest">
            <AlertTriangle className="w-4 h-4 animate-bounce" />
            Кризис 2026: Макроэкономическое давление в Казахстане требует мгновенных решений
          </div>
          
          <h1 className="text-4xl sm:text-5xl md:text-7xl font-black text-white leading-[1.05] tracking-tight mb-8 uppercase">
            ОСТАНОВИТЕ УТЕЧКУ <span className="text-transparent bg-clip-text bg-gradient-to-r from-red-500 to-orange-500">30% ВАШЕЙ МАРЖИ.</span><br />
            ГАРАНТИРУЙТЕ РОСТ БИЗНЕСА В 2026 ГОДУ.
          </h1>
          
          <p className="text-lg md:text-xl text-gray-400 font-medium max-w-3xl mx-auto mb-10 leading-relaxed">
            Пока ваши менеджеры на Сейфуллина спят или игнорируют звонки, ИИ-ассистент Sheberlik.ai обрабатывает 100% лидов с TikTok и 2GIS. Первое касание за <span className="text-white font-bold">0.8 секунд</span>.
          </p>

          {/* ИНТЕРАКТИВНЫЙ ДЕФИЦИТ МЕСТ (URG) */}
          <div className="max-w-md mx-auto bg-[#161B22]/60 border border-white/5 p-4 rounded-xl flex justify-around items-center mb-12 shadow-inner">
            <div className="text-center">
              <span className="text-[10px] uppercase tracking-wider text-gray-500 font-bold block mb-1">Свободно лицензий со скидкой</span>
              <span className="text-2xl font-black text-red-500 font-mono animate-pulse">{seatsLeft} ИЗ 3</span>
            </div>
            <div className="h-8 w-[1px] bg-white/10"></div>
            <div className="text-center">
              <span className="text-[10px] uppercase tracking-wider text-gray-500 font-bold block mb-1">Фиксация стоимости</span>
              <span className="text-2xl font-black text-white font-mono">{formatTime(timeLeft)}</span>
            </div>
          </div>

          {/* ВИДЕО-ПРЕФРЕЙМ (VSL) */}
          <div className="w-full aspect-video bg-[#161B22] rounded-2xl border border-white/10 shadow-[0_0_50px_rgba(0,0,0,0.8)] flex flex-col items-center justify-center mb-12 relative overflow-hidden group">
            {vslPlaying ? (
              <div className="absolute inset-0 bg-black flex flex-col items-center justify-center p-6 text-center">
                <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-[#A51D36] mb-4"></div>
                <p className="text-xs font-mono text-gray-400">Инициализация Vapi.ai голосового потока и демонстрации...</p>
                <button onClick={() => setVslPlaying(false)} className="mt-4 text-xs text-red-500 hover:underline">Вернуться к заставке</button>
              </div>
            ) : (
              <>
                <div className="absolute inset-0 bg-gradient-to-t from-black/85 via-black/30 to-transparent group-hover:bg-black/40 transition-colors z-10"></div>
                <div className="relative z-20 flex flex-col items-center p-8">
                  <button 
                    onClick={() => {
                      setVslPlaying(true);
                      addLog('Пользователь запустил демонстрационное VSL-видео.');
                    }}
                    className="w-20 h-20 bg-[#A51D36] hover:bg-red-600 text-white rounded-full flex items-center justify-center pl-1 shadow-[0_0_50px_rgba(165,29,54,0.4)] transform group-hover:scale-110 transition-transform duration-300"
                  >
                    <Play className="w-10 h-10 fill-current" />
                  </button>
                  <p className="mt-6 text-lg font-bold text-white tracking-wide">
                    ВКЛЮЧИТЕ ЗВУК. НАЖМИТЕ PLAY ДЛЯ ПРОСМОТРА КЛИЕНТСКИХ КЕЙСОВ В СНГ
                  </p>
                  <span className="text-xs text-gray-500 font-mono mt-1">Обучающий lo-fi разбор архитектуры // 4:12</span>
                </div>
              </>
            )}
          </div>

          <div className="flex flex-col sm:flex-row gap-4 justify-center items-center">
            <button 
              onClick={() => calculatorRef.current?.scrollIntoView({ behavior: 'smooth' })}
              className="w-full sm:w-auto bg-[#A51D36] hover:bg-red-600 text-white text-lg font-black py-5 px-10 rounded-xl shadow-[0_0_35px_rgba(165,29,54,0.3)] transition-all transform hover:-translate-y-1 flex items-center justify-center gap-3 uppercase"
            >
              Рассчитать потери ОП <Calculator className="w-5 h-5 text-white" />
            </button>
            <button 
              onClick={openCheckout}
              className="w-full sm:w-auto bg-white/5 hover:bg-white/10 text-white text-lg font-bold py-5 px-10 rounded-xl border border-white/10 transition-all uppercase"
            >
              Внедрить систему дожима
            </button>
          </div>
        </section>

        {/* АГИТАЦИОННЫЙ СИМУЛЯТОР ФИНАНСОВЫХ ПОТЕРЬ */}
        <section ref={calculatorRef} className="py-20 border-t border-white/5 scroll-mt-20">
          <div className="text-center mb-12">
            <h2 className="text-3xl md:text-4xl font-black text-white uppercase">Калькулятор финансовой утечки ОП в Алматы</h2>
            <p className="text-gray-400 mt-2">Выявите скрытые потери прибыли из-за человеческого фактора</p>
          </div>

          <div className="bg-[#161B22]/80 border border-white/5 rounded-2xl p-6 md:p-8 shadow-2xl">
            <div className="grid grid-cols-1 md:grid-cols-2 gap-8 mb-8">
              
              {/* Входные ползунки */}
              <div className="space-y-6">
                <div>
                  <div className="flex justify-between text-xs font-semibold mb-2">
                    <span className="text-gray-300">Месячный объем заявок:</span>
                    <span className="text-red-500 font-bold font-mono">{leadsCount} лидов</span>
                  </div>
                  <input 
                    type="range" min="100" max="3000" step="50" value={leadsCount} 
                    onChange={(e) => {
                      setLeadsCount(Number(e.target.value));
                      addLog(`Изменение калькулятора: Месячный объем лидов равен ${e.target.value}`);
                    }}
                    className="w-full h-1.5 bg-gray-700 rounded-lg appearance-none cursor-pointer accent-[#A51D36]"
                  />
                </div>

                <div>
                  <div className="flex justify-between text-xs font-semibold mb-2">
                    <span className="text-gray-300">Количество сотрудников ОП:</span>
                    <span className="text-red-500 font-bold font-mono">{staffCount} человек</span>
                  </div>
                  <input 
                    type="range" min="1" max="10" step="1" value={staffCount} 
                    onChange={(e) => {
                      setStaffCount(Number(e.target.value));
                      addLog(`Изменение калькулятора: Количество менеджеров равно ${e.target.value}`);
                    }}
                    className="w-full h-1.5 bg-gray-700 rounded-lg appearance-none cursor-pointer accent-[#A51D36]"
                  />
                </div>

                <div>
                  <div className="flex justify-between text-xs font-semibold mb-2">
                    <span className="text-gray-300">Месячный ФОТ 1 сотрудника:</span>
                    <span className="text-red-500 font-bold font-mono">{staffSalary.toLocaleString()} ₸</span>
                  </div>
                  <input 
                    type="range" min="150000" max="600000" step="10000" value={staffSalary} 
                    onChange={(e) => {
                      setStaffSalary(Number(e.target.value));
                      addLog(`Изменение калькулятора: Зарплата менеджера установлена на уровне ${e.target.value} KZT`);
                    }}
                    className="w-full h-1.5 bg-gray-700 rounded-lg appearance-none cursor-pointer accent-[#A51D36]"
                  />
                </div>

                <div>
                  <div className="flex justify-between text-xs font-semibold mb-2">
                    <span className="text-gray-300">Средний чек сделки / услуги (KZT):</span>
                    <span className="text-red-500 font-bold font-mono">{leadValue.toLocaleString()} ₸</span>
                  </div>
                  <input 
                    type="range" min="5000" max="100000" step="5000" value={leadValue} 
                    onChange={(e) => {
                      setLeadValue(Number(e.target.value));
                      addLog(`Изменение калькулятора: Стоимость сделки равна ${e.target.value} KZT`);
                    }}
                    className="w-full h-1.5 bg-gray-700 rounded-lg appearance-none cursor-pointer accent-[#A51D36]"
                  />
                </div>
              </div>

              {/* Вывод расчетов */}
              <div className="bg-black/40 border border-white/5 rounded-xl p-6 flex flex-col justify-between">
                <div className="space-y-4">
                  <div className="flex justify-between text-sm text-gray-400">
                    <span>Прямой ФОТ персонала ОП:</span>
                    <span className="text-white font-mono font-bold">{monthlyStaffOverhead.toLocaleString()} ₸</span>
                  </div>
                  <div className="flex justify-between text-sm text-gray-400">
                    <span>Слито лидов человеком (~30%):</span>
                    <span className="text-red-400 font-mono font-bold">{leakedLeads} лидов</span>
                  </div>
                  <div className="flex justify-between text-sm text-gray-400">
                    <span>Потерянная чистая выручка:</span>
                    <span className="text-red-400 font-mono font-bold">{lostRevenue.toLocaleString()} ₸</span>
                  </div>
                  <div className="border-t border-white/5 pt-4 flex justify-between text-base font-bold text-white">
                    <span>Общие издержки ручной системы:</span>
                    <span className="text-red-500 font-mono">{totalCurrentCost.toLocaleString()} ₸</span>
                  </div>
                </div>

                <div className="border-t border-red-500/20 pt-4 mt-6 text-center">
                  <span className="text-[10px] uppercase tracking-widest text-[#A51D36] font-black block mb-1">Реальная экономия с Sheberlik.ai</span>
                  <p className="text-3xl md:text-4xl font-black text-[#A51D36] font-mono">
                    +{netSavings.toLocaleString()} ₸ <span className="text-xs text-gray-500">/ мес</span>
                  </p>
                </div>
              </div>

            </div>

            <div className="p-4 bg-red-500/5 rounded-xl border border-red-500/10 flex gap-3 items-start">
              <AlertTriangle className="w-5 h-5 text-[#A51D36] shrink-0 mt-0.5" />
              <p className="text-xs text-gray-400 leading-relaxed">
                * Потери рассчитываются на основе эмпирических исследований упущенных лидов в СНГ из-за задержки ответов (более 5 минут). Автоматизация сводит этот показатель к нулю.
              </p>
            </div>
          </div>
        </section>

        {/* EPIPHANY BRIDGE СЛАЙДЕР С ТРИАЛ-КЛОУЗОМ (Error 5 Correction) */}
        <section className="py-20 border-t border-white/5">
          <div className="text-center mb-16">
            <h2 className="text-3xl md:text-4xl font-black text-white uppercase">Разрушение барьеров: Откровение Шеберлик</h2>
            <p className="text-gray-400 mt-2">Пошаговая деконструкция вашей воронки продаж и разрушение ложных убеждений</p>
          </div>

          <div className="flex justify-center gap-2 mb-8">
            {[1, 2, 3, 4, 5].map((idx) => (
              <button 
                key={idx} onClick={() => setActiveStage(idx)}
                className={`h-2 rounded-full transition-all duration-300 ${activeStage === idx ? 'w-8 bg-[#A51D36]' : 'w-2 bg-gray-700'}`}
              />
            ))}
          </div>

          <div className="bg-[#161B22] border border-white/5 rounded-2xl p-6 md:p-8 min-h-[385px] flex flex-col justify-between shadow-2xl relative">
            <div>
              {activeStage === 1 && (
                <div className="animate-fade-in">
                  <span className="text-[10px] font-mono text-[#A51D36] uppercase tracking-widest block mb-2">Секрет #1 // Смена Ведра (The Vehicle)</span>
                  <h3 className="text-2xl font-black text-white mb-4">Традиционные сайты мертвы. Истинный проводник — ИИ-ассистент</h3>
                  <div className="space-y-3">
                    <p className="text-sm text-gray-400 leading-relaxed">
                      Большинство предпринимателей в Алматы думают, что для масштабирования им нужен красивый имиджевый сайт. Это иллюзия. Красивые сайты рассеивают внимание и сливают рекламный бюджет в TikTok. 
                    </p>
                    <p className="text-sm text-gray-300 font-medium italic">
                      "Это как пытаться везти тонну кирпичей на спортивном суперкаре. Красиво, но бесполезно. Истинное решение — жесткая, бесшовная ИИ-автоматизация ОП, перехватывающая лиды за миллисекунды."
                    </p>
                  </div>
                </div>
              )}
              {activeStage === 2 && (
                <div className="animate-fade-in">
                  <span className="text-[10px] font-mono text-[#A51D36] uppercase tracking-widest block mb-2">Секрет #2 // Человеческий Фактор (Internal Belief)</span>
                  <h3 className="text-2xl font-black text-white mb-4">Ваши менеджеры не ленивы. Они просто люди</h3>
                  <div className="space-y-3">
                    <p className="text-sm text-gray-400 leading-relaxed">
                      Вы можете бесконечно отправлять ваш ОП на тренинги по продажам, штрафовать их или мотивировать. Но физиология неизменна: человек засыпает, устает, отвлекается на звонки близких и перегружается.
                    </p>
                    <p className="text-sm text-gray-300 font-medium italic">
                      "ИИ-ассистент Vapi.ai не требует мотивации, не просит отпуск и не болеет. Он делает дожим хладнокровно и последовательно 24 часа в сутки."
                    </p>
                  </div>
                </div>
              )}
              {activeStage === 3 && (
                <div className="animate-fade-in">
                  <span className="text-[10px] font-mono text-[#A51D36] uppercase tracking-widest block mb-2">Секрет #3 // Внешний Кризис (External Belief)</span>
                  <h3 className="text-2xl font-black text-white mb-4">Кризис 2026 года — ваш главный рычаг роста</h3>
                  <div className="space-y-3">
                    <p className="text-sm text-gray-400 leading-relaxed">
                      Вы можете оправдывать спад продаж инфляцией или изменением налогов. Но правда в том, что в период кризиса выигрывает тот, кто закупает клиентов дешевле и обрабатывает их быстрее.
                    </p>
                    <p className="text-sm text-gray-300 font-medium italic">
                      "Затраты на привлечение из TikTok падают на 80% по сравнению с Meta. Скорость реакции ИИ перехватывает горячий спрос у медленных конкурентов."
                    </p>
                  </div>
                </div>
              )}
              {activeStage === 4 && (
                <div className="animate-fade-in">
                  <span className="text-[10px] font-mono text-[#A51D36] uppercase tracking-widest block mb-2">Этап 4 // Эквайринг-Триггер</span>
                  <h3 className="text-2xl font-black text-white mb-4">Бесшовная сшивка Kaspi QR</h3>
                  <div className="space-y-3">
                    <p className="text-sm text-gray-400 leading-relaxed">
                      Уберите препятствия при оплате. Наш ИИ автоматически формирует счета по QR и отправляет платежные линки прямо в WhatsApp. Клиент платит мгновенно в один клик.
                    </p>
                  </div>
                </div>
              )}
              {activeStage === 5 && (
                <div className="animate-fade-in">
                  <span className="text-[10px] font-mono text-[#A51D36] uppercase tracking-widest block mb-2">Этап 5 // Юридическая Броня</span>
                  <h3 className="text-2xl font-black text-white mb-4">Абсолютный комплаенс РК на PS.kz</h3>
                  <div className="space-y-3">
                    <p className="text-sm text-gray-400 leading-relaxed">
                      Базы данных, записи звонков и логи согласий шифруются и хранятся исключительно в дата-центрах на территории Республики Казахстан. Полное соответствие Статье 79 КоАП РК.
                    </p>
                  </div>
                </div>
              )}
            </div>

            {/* Триал-Клоуз Кардона (The Bridge) */}
            <div className="mt-8 pt-8 border-t border-white/5 flex flex-col sm:flex-row justify-between items-center gap-4">
              <p className="text-base font-black text-gray-300 italic text-center sm:text-left">
                "Вы увидели достаточно информации для принятия решения?"
              </p>
              <div className="flex gap-3 w-full sm:w-auto">
                <button 
                  onClick={handleTrialCloseYes}
                  className="flex-1 sm:flex-none bg-[#A51D36] hover:bg-red-600 text-white font-extrabold text-sm px-6 py-3 rounded-lg transition-transform transform active:scale-95 shadow-lg shadow-[#A51D36]/30 uppercase"
                >
                  Да, запустить систему
                </button>
                <button 
                  onClick={handleTrialCloseNo}
                  className="flex-1 sm:flex-none bg-white/5 hover:bg-white/10 text-white font-bold text-sm px-6 py-3 rounded-lg border border-white/10 uppercase"
                >
                  {activeStage === 5 ? 'Повторить презентацию' : 'Нет, следующий этап'}
                </button>
              </div>
            </div>

          </div>
        </section>

        {/* ОБЪЕКТИВНАЯ АННИГИЛЯЦИЯ ВОЗРАЖЕНИЙ (OBJ - NO COLLAPSIBLES!) */}
        <section className="py-20 border-t border-white/5">
          <div className="text-center mb-16">
            <h2 className="text-3xl md:text-4xl font-black text-white uppercase">Разрушение сомнений коммерческого директора</h2>
            <p className="text-gray-400 mt-2">Прямые ответы на ключевые B2B-вопросы без замалчивания трудностей</p>
          </div>

          <div className="space-y-12">
            
            {/* Objection 1 */}
            <div className="border-l-4 border-[#A51D36] pl-6 py-2">
              <h3 className="text-xl font-bold text-white mb-2">Возражение #1: «Это слишком дорого для нашего бюджета в Алматы»</h3>
              <p className="text-sm text-gray-400 leading-relaxed mb-4">
                <strong className="text-red-500">Проблема:</strong> Вы правы, 99,970 ₸ — это значительные ежемесячные расходы для малого ТОО. Вы привыкли платить за дешевые шаблоны и надеяться на удачу.
              </p>
              <p className="text-sm text-gray-300 leading-relaxed">
                <strong className="text-emerald-400">Сдвиг парадигмы и Решение:</strong> Но давайте посмотрим правде в глаза. Ваши менеджеры сливают треть горячих лидов из-за задержки ответов. Потери составляют миллионы тенге упущенной выручки каждый месяц. ИИ окупает себя в первую неделю работы, полностью останавливая утечку трафика. Инвестиция в автоматизацию — это не расход, это единственный способ сохранить маржу ТОО.
              </p>
            </div>

            {/* Objection 2 */}
            <div className="border-l-4 border-cyan-500 pl-6 py-2">
              <h3 className="text-xl font-bold text-white mb-2">Возражение #2: «ИИ не сможет общаться так же качественно, как живой человек»</h3>
              <p className="text-sm text-gray-400 leading-relaxed mb-4">
                <strong className="text-red-500">Проблема:</strong> Совершенно верно. Робот не умеет пить кофе, опаздывать, грубить клиентам при плохом настроении и забывать заносить карточки в CRM.
              </p>
              <p className="text-sm text-gray-300 leading-relaxed">
                <strong className="text-emerald-400">Сдвиг парадигмы и Решение:</strong> Голосовое ядро Vapi.ai с задержкой отклика 400 мс распознает контекст, вежливо перехватывает перебивания на русском и казахском языках, мгновенно отправляет КП в WhatsApp и ставит задачи менеджерам. Вы получаете безупречный ОП, работающий без перерывов 24/7.
              </p>
            </div>

            {/* Objection 3 */}
            <div className="border-l-4 border-purple-500 pl-6 py-2">
              <h3 className="text-xl font-bold text-white mb-2">Возражение #3: «У нас нет времени и ресурсов на сложную интеграцию и программирование»</h3>
              <p className="text-sm text-gray-400 leading-relaxed mb-4">
                <strong className="text-red-500">Проблема:</strong> Если вы попытаетесь нанять программистов и собирать n8n-вебхуки самостоятельно, вы потеряете месяцы времени и сотни тысяч тенге.
              </p>
              <p className="text-sm text-gray-300 leading-relaxed">
                <strong className="text-emerald-400">Сдвиг парадигмы и Решение:</strong> Мы поставляем готовую ИИ-инфраструктуру под ключ. Наша команда за 7 дней подключает ваш домен, настраивает интеграции и WhatsApp Business API на сертифицированном хостинге в Казахстане. Вы только получаете отчеты в CRM и Kaspi.
              </p>
            </div>

          </div>
        </section>

        {/* REVERSAL INVERSION CONTRAST MATRIX (RSK) */}
        <section className="py-16 border-t border-white/5">
          <div className="text-center mb-12">
            <h2 className="text-3xl font-black text-white uppercase">Матрица выбора: Сценарии действия</h2>
            <p className="text-gray-400 mt-2">Жесткий математический выбор между бездействием и системным ростом</p>
          </div>

          <div className="grid md:grid-cols-2 gap-8 mb-12">
            
            {/* Column 1: Cost of Inaction */}
            <div className="bg-red-950/10 border border-red-500/15 p-8 rounded-2xl">
              <h3 className="text-xl font-bold text-red-500 mb-4 flex items-center gap-2 uppercase">
                <TrendingDown className="w-6 h-6" /> Стоимость бездействия
              </h3>
              <ul className="space-y-4 text-xs text-gray-300">
                <li className="flex gap-2">
                  <span className="text-red-500 font-bold">✗</span> Слив до 30% входящего рекламного трафика из-за медленных ответов менеджеров.
                </li>
                <li className="flex gap-2">
                  <span className="text-red-500 font-bold">✗</span> Потери выручки ТОО ОП до 2,900,000 ₸ ежемесячно.
                </li>
                <li className="flex gap-2">
                  <span className="text-red-500 font-bold">✗</span> Риск штрафа по ст. 79 КоАП РК до 865,000 KZT за хранение логов лидов за рубежом.
                </li>
                <li className="flex gap-2 border-t border-red-500/10 pt-4 font-mono text-red-400 font-bold">
                  Итоговая годовая потеря: ~34,800,000 ₸
                </li>
              </ul>
            </div>

            {/* Column 2: 10X ROI */}
            <div className="bg-emerald-950/10 border border-emerald-500/15 p-8 rounded-2xl relative overflow-hidden">
              <div className="absolute top-0 right-0 bg-emerald-500 text-black text-[9px] font-black uppercase px-3 py-1 rounded-bl-xl font-mono">
                Рекомендовано
              </div>
              <h3 className="text-xl font-bold text-emerald-400 mb-4 flex items-center gap-2 uppercase">
                <TrendingUp className="w-6 h-6" /> 10X ROI СИСТЕМА
              </h3>
              <ul className="space-y-4 text-xs text-gray-300">
                <li className="flex gap-2">
                  <span className="text-emerald-500 font-bold">✓</span> Моментальный автодозвон Vapi.ai за 0.8 секунд после лида из TikTok.
                </li>
                <li className="flex gap-2">
                  <span className="text-emerald-500 font-bold">✓</span> 100% занесение стенограммы и данных о звонках роботом в Kommo CRM.
                </li>
                <li className="flex gap-2">
                  <span className="text-emerald-500 font-bold">✓</span> Локализованный, безопасный суверенный бекенд на серверах в Алматы (PS.kz).
                </li>
                <li className="flex gap-2 border-t border-emerald-500/10 pt-4 font-mono text-emerald-400 font-bold">
                  Итоговая годовая выгода: +34,800,000 ₸
                </li>
              </ul>
            </div>

          </div>

          {/* Reversal Inversion Disclaimer */}
          <div className="bg-[#161B22] border-2 border-red-500/20 rounded-3xl p-8 relative overflow-hidden shadow-2xl flex flex-col md:flex-row items-center gap-8">
            <div className="shrink-0 w-24 h-24 rounded-full border-4 border-red-500/40 flex flex-col items-center justify-center p-2 relative bg-black/50 select-none shadow-[0_0_30px_rgba(239,68,68,0.15)]">
              <Award className="w-10 h-10 text-red-500" />
              <span className="text-[8px] font-mono text-center font-bold text-gray-400 mt-1">10X RULE</span>
            </div>
            <div className="flex-grow space-y-2">
              <h4 className="text-lg font-black text-white uppercase">Уведомление об ответственности сторон</h4>
              <p className="text-xs text-gray-400 leading-relaxed">
                Мы берем все технические риски интеграции на себя. Однако, вы должны осознавать: **успех — это ваша обязанность, долг и ответственность**. ИИ — это совершенный инструмент удержания внимания, но результат зависит от вашей готовности перестроить бизнес-процессы компании. Если вы ищете волшебную таблетку без вашего вовлечения — данный продукт вам не подходит.
              </p>
            </div>
          </div>
        </section>

        {/* МАТЕМАТИЧЕСКИЙ VALUE STACK (VS) */}
        <section id="value-stack" ref={valueStackRef} className="py-16 border-t border-white/5 scroll-mt-20">
          <div className="bg-[#161B22]/45 border border-[#A51D36]/15 rounded-3xl p-8 md:p-12 relative overflow-hidden shadow-2xl">
            <div className="absolute top-0 right-0 p-4">
              <span className="text-xs font-mono text-[#A51D36] bg-[#A51D36]/10 px-3 py-1 rounded-full border border-[#A51D36]/20 uppercase tracking-widest font-black">
                Скидка 88%
              </span>
            </div>

            <h2 className="text-3xl md:text-4xl font-black text-white text-center mb-10 uppercase">6-компонентная декомпозиция оффера:</h2>
            
            <div className="space-y-6 mb-12">
              <div className="flex justify-between items-center border-b border-white/5 pb-4">
                <div>
                  <h4 className="font-bold text-white text-lg">1. Opportunity Switch: ИИ-Сшивка с TikTok API</h4>
                  <p className="text-xs text-gray-500 mt-0.5">Полный перехват лидов и автоматизация лид-форм.</p>
                </div>
                <span className="text-red-500 font-mono line-through font-semibold">150 000 ₸</span>
              </div>

              <div className="flex justify-between items-center border-b border-white/5 pb-4">
                <div>
                  <h4 className="font-bold text-white text-lg">2. The Tools: Тюнинг и кастом-промптинг Vapi.ai</h4>
                  <p className="text-xs text-gray-500 mt-0.5">SIP-телефония с локальной авторизацией номеров (+7).</p>
                </div>
                <span className="text-red-500 font-mono line-through font-semibold">250 000 ₸</span>
              </div>

              <div className="flex justify-between items-center border-b border-white/5 pb-4">
                <div>
                  <h4 className="font-bold text-white text-lg">3. Tangible #1: n8n Сценарии + Kommo CRM Сшивка</h4>
                  <p className="text-xs text-gray-500 mt-0.5">Автоматическая запись стенограмм и задач менеджерам.</p>
                </div>
                <span className="text-red-500 font-mono line-through font-semibold">300 000 ₸</span>
              </div>

              <div className="flex justify-between items-center border-b border-white/5 pb-4">
                <div>
                  <h4 className="font-bold text-white text-lg">4. Tangible #2: Готовый пакет скриптов дожима Кардона</h4>
                  <p className="text-xs text-gray-500 mt-0.5">Встроенные ИИ-реплики преодоления барьера «Подумаю».</p>
                </div>
                <span className="text-red-500 font-mono line-through font-semibold">100 000 ₸</span>
              </div>

              <div className="flex justify-between items-center border-b border-white/5 pb-4">
                <div>
                  <h4 className="font-bold text-white text-lg">5. Tangible #3: Эквайринг-шлюз Kaspi Pay Headless</h4>
                  <p className="text-xs text-gray-500 mt-0.5">Мгновенная отправка QR счетов в WhatsApp.</p>
                </div>
                <span className="text-red-500 font-mono line-through font-semibold">150 000 ₸</span>
              </div>

              <div className="flex justify-between items-center border-b border-white/5 pb-4">
                <div>
                  <h4 className="font-bold text-white text-lg">6. Exclusive Bonus: Аудит ОП от ведущего архитектора</h4>
                  <p className="text-xs text-gray-500 mt-0.5">1-on-1 Strategy & Implementation Call для быстрой интеграции.</p>
                </div>
                <span className="text-red-500 font-mono line-through font-semibold">100 000 ₸</span>
              </div>
            </div>

            <div className="flex justify-between items-center mb-12 font-mono">
              <span className="text-xl font-bold text-gray-400">Суммарная ценность пакета:</span>
              <span className="text-2xl font-black text-red-500 line-through">1,050,000 ₸</span>
            </div>

            {/* Карточка тарифа с таймером обратного отсчета */}
            <div className="text-center bg-black/60 p-8 rounded-2xl border border-[#A51D36]/20 shadow-inner relative">
              <div className="absolute top-[-15px] inset-x-0 flex justify-center">
                <span className="bg-[#A51D36] text-white text-[10px] font-bold px-4 py-1 rounded-full uppercase tracking-widest border border-red-500 animate-bounce">
                  Только {seatsLeft} слота со скидкой!
                </span>
              </div>

              <p className="text-xs uppercase tracking-[0.2em] text-[#A51D36] font-black mb-1 mt-4">Месячное абонентское обслуживание воронки</p>
              <p className="text-6xl font-black text-white font-mono mb-6">
                99,970 ₸ <span className="text-2xl text-gray-500 font-medium">/ мес</span>
              </p>
              
              <button 
                onClick={openCheckout}
                className="w-full bg-gradient-to-r from-red-600 to-orange-500 text-white text-xl md:text-2xl font-black py-6 px-8 rounded-xl shadow-[0_0_50px_rgba(165,29,54,0.4)] transition-all transform hover:scale-[1.02] flex items-center justify-center gap-3 active:scale-95 uppercase"
              >
                ЗАПУСТИТЬ ИИ-ИНФРАСТРУКТУРУ <ArrowRight className="w-6 h-6" />
              </button>
              <p className="mt-4 text-xs text-gray-500">Таймер фиксации акционной стоимости: <span className="text-white font-mono font-bold">{formatTime(timeLeft)}</span></p>
            </div>
          </div>
        </section>

      </main>

      {/* 2-STEP CHECKOUT SYSTEM WITH INTERACTIVE BUMP OFFER (Error 2 Correction) */}
      {checkoutModal && (
        <div className="fixed inset-0 bg-black/95 backdrop-blur-md z-50 flex items-center justify-center p-4">
          <div className="bg-[#161B22] border border-white/10 rounded-2xl w-full max-w-md overflow-hidden shadow-2xl relative">
            <button 
              onClick={() => setCheckoutModal(false)}
              className="absolute top-4 right-4 text-gray-500 hover:text-white font-bold text-sm select-none"
            >
              ✕
            </button>

            {/* Шаг 1: Контактный интейк (Сбор данных в amoCRM) */}
            {checkoutStep === 'intake' && (
              <div className="p-6 md:p-8">
                <div className="text-center mb-6">
                  <Lock className="w-10 h-10 text-[#A51D36] mx-auto mb-2" />
                  <h3 className="text-xl font-black text-white uppercase">Регистрация контура</h3>
                  <p className="text-xs text-gray-400 mt-1">Шаг 1 из 2: Закрепление контактов в amoCRM</p>
                </div>
                <form onSubmit={handleIntakeSubmit} className="space-y-4">
                  <div>
                    <label className="block text-[10px] uppercase tracking-wider text-gray-400 font-bold mb-1">Имя Руководителя:</label>
                    <input 
                      type="text" 
                      required 
                      placeholder="Арман Кусаинов" 
                      value={leadName} 
                      onChange={(e) => setLeadName(e.target.value)} 
                      className="w-full bg-[#0D1117] border border-white/10 rounded-lg p-3 text-sm text-white focus:outline-none focus:border-[#A51D36] transition-colors" 
                    />
                  </div>
                  <div>
                    <label className="block text-[10px] uppercase tracking-wider text-gray-400 font-bold mb-1">Название ТОО / ИП:</label>
                    <input 
                      type="text" 
                      required 
                      placeholder="ТОО Almaty Digital" 
                      value={companyName} 
                      onChange={(e) => setCompanyName(e.target.value)} 
                      className="w-full bg-[#0D1117] border border-white/10 rounded-lg p-3 text-sm text-white focus:outline-none focus:border-[#A51D36] transition-colors" 
                    />
                  </div>
                  <div>
                    <label className="block text-[10px] uppercase tracking-wider text-gray-400 font-bold mb-1">Номер телефона (11 цифр):</label>
                    <input 
                      type="tel" 
                      required 
                      placeholder="+7 701 123 45 67" 
                      value={leadPhone} 
                      onChange={(e) => setLeadPhone(e.target.value)} 
                      className="w-full bg-[#0D1117] border border-white/10 rounded-lg p-3 text-sm text-white focus:outline-none focus:border-[#A51D36] transition-colors" 
                    />
                  </div>
                  <div className="space-y-2 pt-2">
                    <label className="flex gap-2.5 items-start cursor-pointer select-none">
                      <input 
                        type="checkbox" 
                        required 
                        checked={consentPDPL} 
                        onChange={() => setConsentPDPL(!consentPDPL)} 
                        className="mt-0.5 accent-[#A51D36]" 
                      />
                      <span className="text-[9px] text-gray-400 leading-relaxed">Даю согласие на локальное хранение данных в РК на сервере PS.KZ в соответствии со ст. 79 Закона о Персональных данных РК.</span>
                    </label>
                    <label className="flex gap-2.5 items-start cursor-pointer select-none">
                      <input 
                        type="checkbox" 
                        required 
                        checked={aiConsent} 
                        onChange={() => setAiConsent(!aiConsent)} 
                        className="mt-0.5 accent-[#A51D36]" 
                      />
                      <span className="text-[9px] text-gray-400 leading-relaxed">Согласен получать рассылки в WhatsApp и звонки ИИ-ассистента в рамках правил информирования об ИИ ст. 641-1 КоАП РК.</span>
                    </label>
                  </div>
                  <button 
                    type="submit" 
                    className="w-full bg-[#A51D36] hover:bg-red-600 text-white font-extrabold text-sm p-3.5 rounded-lg mt-4 transition-all flex justify-center items-center gap-2 active:scale-95 uppercase"
                  >
                    Перейти к оплате KASPI QR <ArrowRight className="w-4 h-4" />
                  </button>
                </form>
              </div>
            )}

            {/* Шаг 2: Только после фиксации контакта — Выставление счета Kaspi QR с Bump Offer */}
            {checkoutStep === 'billing' && (
              <div className="p-6 md:p-8 text-center animate-fade-in">
                <Smartphone className="w-12 h-12 text-[#A51D36] mx-auto mb-3 animate-pulse" />
                <h3 className="text-lg font-black text-white uppercase">Шаг 2 из 2: Оплата счета</h3>
                <p className="text-xs text-gray-400 mt-1">Отсканируйте Kaspi QR-код для активации лицензии</p>

                {/* BUMP OFFER CONTEXT (MC) */}
                <div 
                  onClick={() => {
                    setBumpSelected(!bumpSelected);
                    addLog(`Пользователь переключил Bump Offer: ${!bumpSelected ? 'Выбран' : 'Отменен'}`);
                  }}
                  className={`my-4 p-4 rounded-xl border text-left cursor-pointer transition-all ${bumpSelected ? 'bg-red-950/20 border-red-500/40' : 'bg-black/30 border-white/5 hover:border-white/10'}`}
                >
                  <div className="flex items-center gap-3">
                    <input 
                      type="checkbox" 
                      checked={bumpSelected}
                      onChange={() => {}} // Управляется кликом по контейнеру
                      className="accent-[#A51D36]"
                    />
                    <div className="flex-grow">
                      <span className="text-[10px] font-mono text-[#A51D36] font-black uppercase tracking-wider block mb-0.5">ВЫГОДНОЕ ДОПОЛНЕНИЕ (BUMP)</span>
                      <h4 className="text-xs font-bold text-white leading-tight">Добавить Экспресс-Пакет Скриптов Дожима ОП (+15,000 ₸)</h4>
                      <p className="text-[9px] text-gray-400 mt-1 leading-relaxed">
                        Полная ИИ-сборка скриптов дожима Кардона для преодоления возражений. Обычная стоимость: <span className="line-through">50,000 ₸</span>. Оплатите в этом чеке всего за 15,000 ₸.
                      </p>
                    </div>
                  </div>
                </div>
                
                {/* QR Элемент */}
                <div className="my-6 w-40 h-40 bg-white p-3.5 rounded-xl mx-auto flex flex-col items-center justify-center border-4 border-red-500 relative overflow-hidden select-none">
                  <div className="absolute inset-x-0 h-0.5 bg-red-500 animate-bounce top-0"></div>
                  <div className="grid grid-cols-4 gap-1.5 w-full h-full opacity-70">
                    <div className="bg-black rounded-xs"></div><div className="bg-transparent"></div><div className="bg-black"></div><div className="bg-black"></div>
                    <div className="bg-transparent"></div><div className="bg-black"></div><div className="bg-transparent"></div><div className="bg-transparent"></div>
                    <div className="bg-black"></div><div className="bg-transparent"></div><div className="bg-black"></div><div className="bg-transparent"></div>
                    <div className="bg-black"></div><div className="bg-black"></div><div className="bg-transparent"></div><div className="bg-black"></div>
                  </div>
                  <div className="absolute bg-black px-2 py-1 text-[8px] tracking-widest text-white border border-white/10 rounded font-mono font-bold">KASPI_PAY_QR</div>
                </div>

                {/* Динамическое отображение итоговой стоимости счета */}
                <div className="mb-4 text-center">
                  <span className="text-[10px] text-gray-500 uppercase tracking-wider block">Итого к оплате в Kaspi:</span>
                  <strong className="text-2xl font-black text-white font-mono">
                    {(bumpSelected ? basePrice + bumpPrice : basePrice).toLocaleString()} ₸
                  </strong>
                </div>

                <div className="space-y-4">
                  <button 
                    onClick={simulatePaymentSuccess}
                    className="w-full bg-emerald-600 hover:bg-emerald-500 text-white font-extrabold text-xs py-3 rounded-lg flex items-center justify-center gap-2 transition-all active:scale-95"
                  >
                    ИМИТИРОВАТЬ УСПЕШНУЮ ОПЛАТУ (для демо)
                  </button>
                  <div className="flex items-center justify-center gap-2 text-[10px] font-mono text-gray-500">
                    <RefreshCw className="w-3.5 h-3.5 animate-spin text-[#A51D36]" />
                    <span>Ожидание транзакционного вебхука от Kaspi API...</span>
                  </div>
                </div>
              </div>
            )}

            {/* Шаг 3: Полная победа, запуск WhatsApp-цепочки */}
            {checkoutStep === 'success' && (
              <div className="p-6 md:p-8 text-center animate-fade-in">
                <CheckCircle2 className="w-12 h-12 text-emerald-400 mx-auto mb-3" />
                <h3 className="text-xl font-black text-white">Транзакция Успешна</h3>
                <p className="text-xs text-gray-400 mt-1">Событие "PAYMENT_SUCCESS" зафиксировано на сервере PS.KZ</p>
                
                <div className="my-5 p-4 bg-black/40 rounded-xl border border-white/5 text-left text-[9px] font-mono space-y-1">
                  <p className="text-gray-500">// Метаданные транзакции n8n:</p>
                  <p className="text-[#A51D36]">org_name: "{companyName}"</p>
                  <p className="text-emerald-400">client_phone: "{leadPhone}"</p>
                  <p className="text-emerald-400">license_allocated: "ACTIVE_10X"</p>
                  <p className="text-emerald-400">bump_offer_added: "{bumpSelected ? 'YES' : 'NO'}"</p>
                </div>

                <div className="bg-emerald-500/5 border border-emerald-500/10 p-3.5 rounded-xl text-left flex gap-2.5 items-start mb-6">
                  <ShieldCheck className="w-5 h-5 text-emerald-400 shrink-0 mt-0.5" />
                  <p className="text-[10px] text-gray-400 leading-relaxed">
                    <strong className="text-emerald-400 font-bold block mb-0.5">ВНИМАНИЕ РУКОВОДИТЕЛЮ:</strong> Сценарий n8n отправил пакет инструкций в WhatsApp. Первичный звонок от ИИ-помощника поступит в течение 60 секунд.
                  </p>
                </div>

                <button 
                  onClick={() => setCheckoutModal(false)}
                  className="w-full bg-[#0D1117] hover:bg-black text-white text-xs font-bold p-3 rounded-lg border border-white/10"
                >
                  Вернуться в воронку
                </button>
              </div>
            )}

          </div>
        </div>
      )}

      {/* FOOTER: n8n ПАЙПЛАЙН И КОНСОЛЬ */}
      <footer className="w-full bg-[#161B22]/95 border-t border-white/10 p-4 font-mono text-[10px] backdrop-blur-md flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 z-30">
        <div className="flex items-center gap-3 shrink-0">
          <span className="w-2.5 h-2.5 rounded-full bg-emerald-500 animate-ping"></span>
          <span className="text-gray-300 font-bold tracking-wider text-[9px]">N8N PIPELINE & PDPL AUDITOR:</span>
        </div>
        <div className="flex-grow overflow-x-auto whitespace-nowrap text-emerald-400 scrollbar-none text-[9px]" id="audit-log">
          {logs[logs.length - 1]}
        </div>
        <div className="flex gap-2 shrink-0">
          <span className="px-2 py-1 bg-white/5 rounded text-gray-400 text-[8px] border border-white/5">PROD_MODE</span>
          <span className="px-2 py-1 bg-[#A51D36]/10 text-[#A51D36] text-[8px] rounded border border-[#A51D36]/20 font-bold">ALMATY VPS OK</span>
        </div>
      </footer>

    </div>
  );
}